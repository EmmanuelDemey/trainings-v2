// Step 3 — the data module.
//
// TODO: move `items` and the four functions around it here, and export
// getItems, addItem, removeItem and total. `items` itself stays private: the
// outside world only ever goes through the functions.
//
// Step 4 — add this line at the top level of the file, then import store.js
// from BOTH app.js and cart-item.js. Count how many times it is printed.
// console.log('store loaded');
