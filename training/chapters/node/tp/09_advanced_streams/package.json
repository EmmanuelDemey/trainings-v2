{
  "name": "tp-09-advanced-streams",
  "private": true,
  "type": "module",
  "engines": { "node": ">=24" },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "producer": "node src/amqp/producer.ts",
    "consumer": "node src/amqp/consumer.ts",
    "pub": "node src/redis/publisher.ts",
    "sub": "node src/redis/subscriber.ts",
    "backpressure": "node src/backpressure.ts",
    "infra:up": "docker compose up -d",
    "infra:down": "docker compose down"
  },
  "dependencies": {
    "amqplib": "^0.10.0",
    "ioredis": "^5.4.0"
  },
  "devDependencies": {
    "typescript": "^5.8.0",
    "@types/node": "^24.0.0",
    "@types/amqplib": "^0.10.0"
  }
}
