# Copy to `.env.local` and adjust. Anything prefixed VITE_ ends up in the
# JavaScript bundle and is readable by anyone — never put a secret here.
VITE_API_URL=/api
VITE_FEATURE_REPORTS=false
VITE_SENTRY_DSN=
